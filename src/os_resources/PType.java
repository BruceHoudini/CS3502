package os_resources;

//Used by the loader to assign and identify control card strings
//Definitely should have been named CCType for "Control Card" Type
public enum PType {
	JOB, DATA, END_PROC, EOF
}
